# SMS-automate-to-DB-
This task to automate the receive SMS message to update the google sheet and then connect the MYSQL Database 
Use to the IFTTT app install and connect to receive sms and google sheet use to the app script to automatic this process paste the sms_log code and deploy get the link. that link to fetc the data help of google sheet 
To receive the sms in phone to send the google sheet and that google sheet data to use app script to storage the data in MYSQL Database 
and to Automate to push the data in mysql using python code to every 30 sec to fetch the google sheet data 
